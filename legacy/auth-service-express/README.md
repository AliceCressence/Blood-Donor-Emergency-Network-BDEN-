# Legacy Express Auth Prototype

This folder contains the original Express authentication prototype. It is preserved for reference only because it was teammate work and may contain useful context.

Do not run this service as part of the active BDEN backend, and do not point the frontend at it.

The active authentication implementation lives in:

```text
services/auth-service/
```
