server {
    listen 80;
    server_name bden.hinkaku.tech 3.77.183.190;

    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:30080;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect http://$host:30080/ $scheme://$host/;
        proxy_redirect http://bden.hinkaku.tech:30080/ $scheme://bden.hinkaku.tech/;
        proxy_redirect http://127.0.0.1:30080/ $scheme://$host/;
        proxy_redirect http://localhost:30080/ $scheme://$host/;
        proxy_redirect http://3.77.183.190:30080/ $scheme://$host/;
        proxy_read_timeout 60s;
        proxy_connect_timeout 10s;
    }
}

server {
    listen 80;
    server_name jenkins.bden.hinkaku.tech;

    location / {
        proxy_pass http://127.0.0.1:8090;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_request_buffering off;
        proxy_read_timeout 90s;
    }
}

server {
    listen 80;
    server_name grafana.bden.hinkaku.tech;

    location / {
        proxy_pass http://127.0.0.1:3002;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
